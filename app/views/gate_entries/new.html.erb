<% content_for :title, "New Gate Entry" %>

<div class="page-header">
  <div>
    <h1>New Gate Entry</h1>
    <p class="page-subtitle">Log a newly arrived vehicle at the gate.</p>
  </div>
  <div>
    <%= link_to "Back to list", gate_entries_path, class: "btn btn-secondary" %>
  </div>
</div>

<div class="card">
  <%= render "form", gate_entry: @gate_entry %>
</div>
