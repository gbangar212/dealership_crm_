<% content_for :title, "New Role" %>

<div class="page-header">
  <div>
    <h1>New Role</h1>
    <p class="page-subtitle">Add a new role for access control.</p>
  </div>
  <div>
    <%= link_to "Back to roles", roles_path, class: "btn btn-secondary" %>
  </div>
</div>

<div class="card">
  <%= render "form", role: @role %>
</div>
