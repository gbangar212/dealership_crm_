name: dealership_crm

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    environment:
      RAILS_ENV: development
    volumes:
      - .:/app
      - bundle:/usr/local/bundle
    tty: true
    stdin_open: true

volumes:
  bundle:
